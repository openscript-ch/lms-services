log:
  level: INFO

api:
  insecure: false
  dashboard: true

entryPoints:
  http:
    address: ":80"
  https:
    address: ":443"

certificatesResolvers:
  defaultResolver:
    acme:
      email: "#{ACME_EMAIL}#"
      storage: "/letsencrypt/acme.json"
      httpChallenge:
        entryPoint: "http"